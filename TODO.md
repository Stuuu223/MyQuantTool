# TODO - MyQuantTool 待办事项

## 🔥 高优先级

### 修改 equity_info_tushare 存储结构以支持历史查询

**状态**: 待实施  
**优先级**: P0（阻塞 intraday 扫描功能）  
**预计工作量**: 1-2 小时

**问题链**:
```
缺少 circ_mv 
  ↓
无法计算 ratio (主力净流入/流通市值)
  ↓
FOCUS 标记不完整
  ↓
所有股票被标记为 BLOCK❌
```

**当前问题**:
- ❌ 存储结构: `{ts_code: {float_mv, ...}}` - 只存储最新数据
- ❌ 无法查询历史日期的流通市值
- ❌ 无法在 intraday 扫描时按 trade_date join

**目标结构**:
```python
{
    "latest_update": "20250813",
    "retention_days": 30,
    "data": {
        "20250813": {
            "603607.SH": {"float_mv": 1000000000, ...},
            "000001.SZ": {"float_mv": 500000000, ...}
        },
        "20250812": {...}
    }
}
```

**实现清单**:
- [ ] 修改存储格式为 `{trade_date: {ts_code: data}}`
- [ ] 实现增量更新逻辑（只获取缺失日期）
- [ ] 添加数据清理功能（保留最近 30 天）
- [ ] 提供查询接口 `get_circ_mv(ts_code, trade_date)`
- [ ] 更新 intraday 扫描脚本以使用新接口
- [ ] 测试历史日期查询和数据 join

**相关文件**:
- `tasks/sync_equity_info_tushare.py`
- `data/equity_info_tushare.json`
- `tasks/run_event_driven_monitor.py`
- `logic/full_market_scanner.py`

**验证方式**:
- 使用 `tasks/run_event_driven_monitor.py --replay` 复盘历史数据
- 验证 FOCUS 标签包含正确的 `ratio` 计算
- 确认占比字段显示为实际数值（非 "--"）

---

## 📝 中优先级

（暂无）

---

## 🔍 低优先级

（暂无）

---

*最后更新：2026-02-07*