# UI模块使用说明

## 概述

本项目已完成代码结构优化，将原本4685行的main.py拆分为19个独立的UI模块。

## 目录结构

```
ui/
├── __init__.py              # 模块入口，导出所有渲染函数
├── single_stock.py          # 单股分析（完整实现）
├── multi_compare.py         # 多股对比（完整实现）
├── backtest.py              # 策略回测（框架）
├── sector_rotation.py       # 板块轮动（完整实现）
├── long_hu_bang.py          # 龙虎榜（框架）
├── dragon_strategy.py       # 龙头战法（框架）
├── auction.py               # 集合竞价（框架）
├── sentiment.py             # 情绪分析（框架）
├── hot_topics.py            # 热点题材（框架）
├── alert.py                 # 智能预警（框架）
├── volume_price.py          # 量价关系（框架）
├── ma_strategy.py           # 均线战法（框架）
├── new_stock.py             # 次新股（框架）
├── capital.py               # 游资席位（框架）
├── limit_up.py              # 打板预测（框架）
├── smart_recommend.py       # 智能推荐（框架）
├── risk.py                  # 风险管理（框架）
├── history.py               # 历史记录（框架）
└── settings.py              # 系统设置（框架）
```

## 使用方法

### 1. 使用新的模块化入口（推荐）

```bash
streamlit run main_new.py
```

### 2. 在代码中导入

```python
from ui.single_stock import render_single_stock_tab
from ui.multi_compare import render_multi_compare_tab
from ui.sector_rotation import render_sector_rotation_tab

# 渲染各个标签页
with tab_single:
    render_single_stock_tab(db, config)

with tab_compare:
    render_multi_compare_tab(db, config)

with tab_sector:
    render_sector_rotation_tab(db, config)
```

## 模块状态

### 已完成（3个）
- ✅ single_stock.py - 单股分析（完整实现）
- ✅ multi_compare.py - 多股对比（完整实现）
- ✅ sector_rotation.py - 板块轮动（完整实现）

### 框架待完善（16个）
- ⏳ backtest.py - 策略回测
- ⏳ long_hu_bang.py - 龙虎榜
- ⏳ dragon_strategy.py - 龙头战法
- ⏳ auction.py - 集合竞价
- ⏳ sentiment.py - 情绪分析
- ⏳ hot_topics.py - 热点题材
- ⏳ alert.py - 智能预警
- ⏳ volume_price.py - 量价关系
- ⏳ ma_strategy.py - 均线战法
- ⏳ new_stock.py - 次新股
- ⏳ capital.py - 游资席位
- ⏳ limit_up.py - 打板预测
- ⏳ smart_recommend.py - 智能推荐
- ⏳ risk.py - 风险管理
- ⏳ history.py - 历史记录
- ⏳ settings.py - 系统设置

## 迁移步骤

要将原main.py中的功能迁移到对应模块：

1. **读取原main.py**中对应Tab的代码
2. **复制到对应UI模块**中
3. **修改导入语句**，确保所有依赖都已导入
4. **测试功能**，确保迁移后功能正常
5. **提交代码**

## 示例：迁移龙虎榜模块

```python
# 1. 读取 main.py 中 tab_lhb 的代码（约1936-2156行）
# 2. 复制到 ui/long_hu_bang.py
# 3. 添加必要的导入
from logic.algo_advanced import AdvancedAlgo
from logic.formatter import Formatter

def render_long_hu_bang_tab(db, config):
    """渲染龙虎榜标签页"""
    st.subheader("🏆 龙虎榜分析")
    # ... 原有代码 ...
```

## 优势

1. **代码清晰**：每个模块独立，职责单一
2. **易于维护**：修改某个功能只需修改对应模块
3. **便于扩展**：添加新功能只需创建新模块
4. **团队协作**：不同开发者可以同时开发不同模块
5. **测试友好**：每个模块可以独立测试

## 注意事项

1. 所有UI模块的渲染函数都应该接收 `db` 和 `config` 参数
2. 避免在模块中创建全局变量
3. 使用 `st.spinner` 显示加载状态
4. 使用 `st.error`、`st.warning`、`st.success` 显示操作结果
5. 保持代码风格一致

## 下一步计划

1. 逐步将剩余16个模块的代码从main.py迁移过来
2. 完善每个模块的具体实现
3. 添加单元测试
4. 最终用main_new.py替换main.py