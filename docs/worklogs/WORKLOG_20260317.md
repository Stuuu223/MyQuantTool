# MyQuantTool Worklog - 2026-03-17

## 一、今日工作概述

继续修复上一会话遗留的P0-P2级别问题，核心聚焦于**卖出价格错误**和**决策数据完整性**两大问题域。

### 1.1 任务清单

| 任务ID | 优先级 | 问题描述 | 状态 |
|--------|--------|----------|------|
| P0-1 | 高 | 卖出价格全为¥5.xx，明显错误 | ✅ 已修复 |
| P0-2 | 高 | median_score字段全为0 | ✅ 已修复 |
| P0-3 | 高 | Bug#4注入时机错误 | ✅ 之前已修复 |
| P1-4 | 中 | ignition_prob全为0导致通道B死锁 | ✅ 已修复 |
| P2-5 | 低 | 车轮战行为（卖出后立即买入） | ✅ 已修复 |
| P2-6 | 低 | 报告诚信性验证 | ✅ 验证通过 |

---

## 二、P0任务1：卖出价格错误修复

### 2.1 问题现象

战报JSON中所有卖出价格都显示为¥5.xx，与实际持仓价格不符：
```json
{
  "stock_code": "000677.SZ",
  "buy_price": 7.40,
  "sell_price": 5.36,  // 错误！应该接近7.xx
  "pnl_pct": -27.57%
}
```

### 2.2 问题诊断

添加Debug日志追踪：
```python
logger.info(f"[DEBUG SELL] target_price={target_price} | pos.current_price={pos.current_price}")
```

发现关键线索：
```
[DEBUG SELL] target_price=4.31 | pos.current_price=0.00
```

**根因定位**：
1. 真实引擎(MockExecutionManager)拒绝了买入（涨停买不进）
2. PaperTradeEngine成功买入（零摩擦）
3. 决策大脑状态显示`held_stock_code=000677.SZ`
4. 但`held_price`从真实引擎获取，真实引擎没有持仓
5. 导致`held_price`使用了错误的默认值

### 2.3 修复方案

**文件**: `tools/mock_live_runner.py`

```python
# ── Phase B: 决策大脑
# 【P0修复】从正确的引擎获取held_price
held_price = 0.0
brain_held_code = self.decision_brain.held_stock_code

if brain_held_code:
    # 优先从真实引擎获取
    if brain_held_code in self.execution_manager.positions:
        pos = self.execution_manager.positions[brain_held_code]
        held_price = pos.current_price if pos.current_price > 0 else pos.entry_price
    # 其次从PaperTrade引擎获取
    elif brain_held_code in self.paper_engine.positions:
        held_price = self.paper_engine.positions[brain_held_code].get('current_price', 0.0)
        if held_price <= 0:
            held_price = self.paper_engine.positions[brain_held_code].get('cost_price', 0.0)
    # 最后从榜单获取（最可靠）
    target = next((t for t in enriched_targets if t['code'] == brain_held_code), None)
    if target and target.get('price', 0) > 0:
        held_price = target['price']
```

### 2.4 验证结果

修复后卖出价格正确：
```json
{
  "stock_code": "000677.SZ", 
  "buy_price": 7.40,
  "sell_price": 7.85,  // 正确！
  "pnl_pct": +6.08%
}
```

**Commit**: `5ad660d`

---

## 三、P0任务2：median_score全为0修复

### 3.1 问题现象

战报中所有决策的`median_score`字段都是0：
```json
{
  "action": "SELL",
  "median_score": 0,  // 错误！
  "p90_threshold": 0   // 错误！
}
```

### 3.2 问题诊断

查看`trade_decision_brain.py`的`on_new_frame`方法：

```python
if decision.action == 'BUY':
    decision.p90_threshold = p90_threshold
    decision.median_score = median_score
    return decision
```

**根因定位**：
- BUY分支正确填充了`median_score`和`p90_threshold`
- 但SELL分支和HOLD分支没有填充这些字段
- 导致这些字段使用默认值0

### 3.3 修复方案

**文件**: `logic/execution/trade_decision_brain.py`

在SELL分支添加：
```python
decision.action = 'SELL'
decision.stock_code = self.held_stock_code
decision.price = held_stock_current_price
decision.reason = exit_decision[1]
decision.score = self.entry_score
# 【P0修复】SELL分支也填充p90和median（使用缓存值）
decision.p90_threshold = self._last_frame_p90
decision.median_score = self._last_frame_median
```

在HOLD分支添加：
```python
decision.action = 'HOLD'
decision.stock_code = self.held_stock_code
decision.price = held_stock_current_price
decision.score = self.entry_score
# 【P0修复】HOLD分支也填充p90和median
decision.p90_threshold = self._last_frame_p90
decision.median_score = self._last_frame_median
```

**Commit**: `6d443ed`

---

## 四、P1任务4：ignition_prob字段名不匹配修复

### 4.1 问题现象

通道B入场条件：
```python
ignition_prob >= entry_ignition_prob_min  # 20%
```

但所有决策的`ignition_prob`都是0，导致通道B永远无法触发。

### 4.2 问题诊断

**根因定位**：字段名不匹配

1. `kinetic_core_engine.py`输出的字段名是`ignition_probability_pct`
2. `mock_live_runner.py`读取的字段名是`ignition_prob`
3. 字段名不一致，导致取值失败，默认返回0

### 4.3 修复方案

**文件**: `tools/mock_live_runner.py`

```python
dbg = data.get('debug_metrics', {})
ignition_prob = dbg.get('ignition_probability_pct', 0.0)  # 【P1修复】字段名匹配
```

**Commit**: `a1424e5`

---

## 五、P2任务5：30分钟冷静期防止车轮战

### 5.1 问题现象

系统出现"车轮战"行为：
- 09:45 卖出股票A
- 09:46 立即买入股票B
- 09:55 卖出股票B
- 09:56 立即买入股票C

这种高频交易不符合右侧追涨策略的设计初衷。

### 5.2 修复方案

**文件**: `logic/execution/trade_decision_brain.py`

新增属性：
```python
# 【P2修复】卖出后冷静期：防止车轮战
self._last_sell_time: Optional[datetime] = None
self._sell_cooldown_minutes: int = config.get('sell_cooldown_minutes', 30)
```

在`clear_position`中记录卖出时间：
```python
def clear_position(self):
    self.held_stock_code = None
    self.entry_score = None
    self.entry_time = None
    self._last_sell_time = datetime.now()  # 【P2修复】记录卖出时间
```

在`_should_enter`中检查冷静期：
```python
def _should_enter(self, current_time: datetime, ...):
    # 【P2修复】冷静期检查
    if self._last_sell_time is not None:
        minutes_since_sell = (current_time - self._last_sell_time).total_seconds() / 60
        if minutes_since_sell < self._sell_cooldown_minutes:
            remaining = self._sell_cooldown_minutes - minutes_since_sell
            logger.debug(f"[冷静期] 卖出后{minutes_since_sell:.0f}分钟，还需等待{remaining:.0f}分钟")
            return None
```

**Commit**: `1d558d5`

---

## 六、P2任务6：报告诚信性验证

### 6.1 验证项

| 检查项 | 预期 | 实际 | 结果 |
|--------|------|------|------|
| buy_count与buy_events长度一致 | 3 | 3 | ✅ |
| sell_count与sell_events长度一致 | 2 | 2 | ✅ |
| 卖出价格与持仓价格相关 | ~7.xx | 7.85 | ✅ |
| median_score非零 | >0 | 1124 | ✅ |
| p90_threshold非零 | >0 | 10101 | ✅ |

### 6.2 战报数据示例

```json
{
  "session_id": "run_20260317_092951",
  "summary": {
    "buy_count": 3,
    "sell_count": 2,
    "final_pnl_pct": 6.08
  },
  "sell_events": [
    {
      "stock_code": "000677.SZ",
      "sell_price": 7.85,
      "buy_price": 7.40,
      "pnl_pct": 6.08,
      "median_score": 1124,
      "p90_threshold": 10101
    }
  ]
}
```

---

## 七、Git提交链

```
1d558d5 - P2修复: 添加卖出后30分钟冷静期，防止车轮战行为
a1424e5 - P1修复: ignition_prob字段名不匹配导致通道B死锁
6d443ed - P0修复: SELL/HOLD分支缺失median_score和p90_threshold
5ad660d - P0修复: 卖出价格错误(held_price从错误引擎获取)
```

---

## 八、测试验证

### 8.1 回归测试

```
============================= test session starts =============================
tests/regression/test_all.py .............................           [100%]
============================= 25 passed in 0.46s ==============================
```

### 8.2 集成测试

```
python tools/mock_live_runner.py --date 20260227 --stocks 000001.SZ

结果：
- Tick加载: 4751个 ✅
- 分数计算: 非零 ✅
- 买入决策: 正常 ✅
- 卖出价格: 正确 ✅
- 战报生成: 完整 ✅
```

---

## 九、今日总结

### 9.1 修复统计

| 优先级 | 问题数 | 修复行数 |
|--------|--------|----------|
| P0 | 2 | ~30行 |
| P1 | 1 | ~5行 |
| P2 | 2 | ~20行 |

### 9.2 关键教训

1. **双引擎状态同步**: 真实引擎和PaperTrade引擎可能状态不一致，决策时要明确从哪个引擎获取数据
2. **字段名一致性**: 跨模块字段名要统一，避免`ignition_probability_pct` vs `ignition_prob`这种错误
3. **分支完整性**: if-else所有分支都要处理完整，SELL/HOLD分支缺失字段导致数据丢失

### 9.3 架构改进建议

1. **held_price获取标准化**: 建议封装为`get_held_price()`方法，统一处理多引擎查询
2. **字段名枚举化**: 使用枚举或常量定义字段名，避免字符串硬编码
3. **冷静期配置化**: 当前30分钟冷静期是经验值，建议通过回测验证最优值

---

## 十、遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| entry_min_board_size阈值 | P2 | 当前10可能过严，需验证最优值 |
| 真实Tick数据缺失 | P1 | 20260301-20260315期间数据缺失 |
| 双引擎Alpha分析 | P2 | PaperTrade vs 真实摩擦对比未输出 |

---

*报告生成时间: 2026-03-17*
*Git HEAD: 1d558d52db8c6058f7dd9283758003c5af9dd3e5*
