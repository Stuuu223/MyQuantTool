# WORKLOG 2026-03-18

## 今日提交链

| Commit | 版本 | 核心改动 |
|--------|------|----------|
| `62e74b9` | V199/V200 | 断层碾压狙击逻辑 + 心跳JSONL + 三列透视大屏 |
| `699ef35` | V201 | 狙击手风控大屏与战地黑匣子重构 |
| `5eea8e4` | V202 | 双轨持久化 + 独立心跳快照 + 序列化防爆盾 |
| `d4f8a76` | V204/V205 | LIFECYCLE键名对齐 + 瞬时物理态注入 |

---

## V199/V200 断层碾压狙击逻辑

### 核心改动

**trade_decision_brain.py**
```python
MIN_SNIPER_SCORE = 5000.0  # 最低狙击分数
FAULT_LINE_RATIO = 1.2     # 断层碾压比例

# 条件A：分数必须超过最低狙击线
if top1_score < MIN_SNIPER_SCORE:
    return None  # VETO:分数不足

# 条件C：断层碾压（榜首必须碾压第二名20%以上）
if top2_score > 0 and top1_score < top2_score * FAULT_LINE_RATIO:
    return None  # VETO:未形成断层碾压
```

**universal_tracker.py**
- 新增 `_frame_counter` 帧计数器
- 新增 `HEARTBEAT_INTERVAL=60` 每60帧写心跳
- `_write_heartbeat()`: 遍历registry写入全榜快照
- `write_decision()`: 记录BUY/VETO/HELD决策事件

**metrics_utils.py 三列透视**
| 列名 | 含义 | 示例 |
|-----|------|-----|
| `DIST%` | 涨停逼近率 | `2.3%` |
| `LIFECYCLE` | 时间生命周期 | `09:45(15m)` |
| `BRAIN_STATE` | 大脑状态 | `VETO:未断层` |

---

## V201 狙击手风控大屏

### CTO指控问题

1. **架构降级**：streaming_report被sniper_log替换
2. **HEARTBEAT炸弹**：几百只股票塞进JSONL单行
3. **VETO理由缺失**：return None丢失拒绝理由

### 修复内容

- JSONL文件名：`streaming_report_{date}.jsonl` → `sniper_log_{date}.jsonl`
- 事件类型：EVENT_APPEAR / EVENT_VETO / EVENT_FIRE / HEARTBEAT
- VETO返回值：从`return None`改为返回带理由的字典

---

## V202 双轨持久化

### 核心架构

```
data/battle_reports/
├── streaming_report_20260318.jsonl    # 轨道1：行情轨迹
├── sniper_log_20260318.jsonl          # 轨道2：决策流
└── heartbeats/                        # 轨道3：心跳快照
    └── snapshot_20260318_100500.json
```

### SafeJSONEncoder 防爆盾

```python
class SafeJSONEncoder(json.JSONEncoder):
    def default(self, obj):
        if isinstance(obj, datetime):
            return obj.isoformat()
        if isinstance(obj, (np.integer, np.floating)):
            return float(obj)
        if isinstance(obj, float):
            if np.isnan(obj) or np.isinf(obj):
                return None
        return super().default(obj)
```

### HEARTBEAT独立快照

- 废除把几百只股票塞进JSONL单行的性能炸弹
- 覆写模式写入 `heartbeats/snapshot_{time}.json`
- 盘后可加载任意时间点的全市场兵力分布

---

## V204 LIFECYCLE键名对齐

### 问题根因

- `metrics_utils.py` 读取：`first_appear_time`
- `run_live_trading_engine.py` 写入：`first_entry_time`

### 修复

```python
# 同时检查两个键名，优先使用first_appear_time
first_appear = t.get('first_appear_time', '') or t.get('first_entry_time', '')
```

---

## V205 瞬时物理态注入

### CTO核心诉求

> "作为狙击系统，没有任何记录比'开枪瞬间的物理环境'更重要。"

### sniper_log 瞬时物理态

```python
return {
    'action': 'VETO',
    'code': top1.get('code'),
    'score': top1_score,
    # 【CTO V205】瞬时物理态字段
    'mfe': top1.get('mfe', 0.0),
    'price_momentum': top1.get('price_momentum', 0.0),
    'inflow_ratio': top1.get('inflow_ratio', 0.0),
    'sustain_ratio': top1.get('sustain_ratio', 0.0),
    'p90_threshold': p90,
    'median_score': median,
}
```

### streaming_report 瞬时截面捕获

```json
{
  "ts": "10:22:57",
  "event": "new_appear",
  "code": "300302.SZ",
  "price_at_signal": 24.83,
  "mfe_at_signal": 2.1,
  "price_momentum_at_signal": 0.98,
  "inflow_ratio_at_signal": 9.44,
  "sustain_ratio_at_signal": 25.32,
  "score": 496093.3
}
```

---

## 回归测试

**25/25 全部通过**

---

## 待解决问题

1. **Rich Live原地刷新**：CTO要求使用`rich.live.Live`替代`console.clear()`，需重构主循环
2. **实盘引擎调用验证**：streaming_report_20260318.jsonl只有测试数据，需验证实盘引擎是否正确调用UniversalTracker
3. **盘口L1深度数据**：CTO要求预留depth_ratio字段用于85% L2贴合度盘口推断

---

## 关键教训

1. **双轨持久化**：行情流（streaming_report）和决策流（sniper_log）必须独立，不能相互替换
2. **瞬时物理态**：记录信号触发时刻的物理环境，而非事后统计
3. **键名一致性**：跨模块字段命名必须统一，避免`first_appear_time` vs `first_entry_time`的分裂
